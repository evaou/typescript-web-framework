# Web Framework

## Setup

    $ npm install -g parcel-bundler
    $ parcel index.html

    $ npm install -g json-server
    $ json-server -w db.json

    $ npm install axios

    $ tsc --init

## Run

    $ npm run start:db
    $ npm run start:parcel
